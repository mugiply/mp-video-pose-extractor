import { PoseDebugPipe } from './pose-debug.pipe';

describe('PoseDebugPipe', () => {
  it('create an instance', () => {
    const pipe = new PoseDebugPipe();
    expect(pipe).toBeTruthy();
  });
});
